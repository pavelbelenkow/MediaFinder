import Foundation

extension CGPoint {
    
    /// Stores default shimmer start point `(0, 0)`
    static let defaultShimmerStartPoint: Self = .zero
    
    /// Stores default shimmer end point `(1, 1)`
    static let defaultShimmerEndPoint: Self = .init(x: 1.0, y: 1.0)
}
